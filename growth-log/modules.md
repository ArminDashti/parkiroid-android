# Growth log — modules

- Capture / camera: duty-cycled still capture in `CaptureService` + `DoganCamera`; continuous bind only for Preview or recording.
- Recording settings: global `recording_enabled` switch (default off).
