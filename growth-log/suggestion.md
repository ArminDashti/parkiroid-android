# Growth log — suggestions

- Consider wrapping export + install into a single Android Studio “run configuration” doc for contributors who prefer the IDE over CLI.
- Consider releasing the partial wake lock between duty-cycle frames so the CPU can sleep more deeply while parked.
