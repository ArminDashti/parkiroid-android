<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="@string/fps_label"
        android:textColor="@color/dogan_on_surface" />

    <ImageButton
        android:id="@+id/fpsMinusBtn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/fps_decrease"
        android:src="@drawable/ic_remove"
        app:tint="@color/dogan_on_surface" />

    <TextView
        android:id="@+id/fpsValueTxt"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/dogan_on_surface"
        android:textSize="16sp" />

    <ImageButton
        android:id="@+id/fpsPlusBtn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/fps_increase"
        android:src="@drawable/ic_add"
        app:tint="@color/dogan_on_surface" />
</LinearLayout>
